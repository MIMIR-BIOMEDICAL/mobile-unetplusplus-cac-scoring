"""Module containing utility function for ml model"""


def node_name_func(i: int, j: int) -> str:
    """
    Function to create node name base on loop variable

    Args:
        i:
        j:

    Returns:

    """
    return str(i) + str(j)
